# PyTorch bindings to sphericart

This package contains the PyTorch bindings for sphericart, with support for
both `torch.compile` and TorchScript. See the
[sphericart documentation](https://sphericart.readthedocs.io/en/latest/) for
more information.
